numpy
pyyaml
reportlab
# Optional — only used if OPENAI_API_KEY is set (API embeddings, no torch):
openai
# Production backends (install only what you connect to):
# presidio-analyzer   presidio-anonymizer   # global NER (names/addresses)
# sentence-transformers                      # local embeddings (needs more RAM)
# pinecone-client | qdrant-client | psycopg[binary]  # vector DB connectors
